services:
 api:
  build: ./backend
  ports:
   - "3000:3000"
  environment:
   NODE_ENV: production

 postgres:
  image: postgres:16
  restart: always
  environment:
   POSTGRES_DB: crm
   POSTGRES_USER: postgres
   POSTGRES_PASSWORD: postgres